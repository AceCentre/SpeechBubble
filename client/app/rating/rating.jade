extends ../../layout/2_column
include ../mixins/results-helpers
block content
  button.btn.btn-primary.pull-right(ng-click='create()', ng-if='isLoggedIn()') Rate
  h1 {{ ratings.product.name }}
  +ratings()

  hr

  div.panel.panel-default(ng-repeat='review in ratings.reviews')
    div.panel-heading
      | {{ review.author.firstName }}
      div.pull-right(rating, ng-model="review.rating", max="5", readonly="true")
    div.panel-body
      | {{ review.comment }}
